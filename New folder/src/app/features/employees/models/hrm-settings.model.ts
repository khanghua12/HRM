export interface DepartmentSetting {
  id: string;
  name: string;
  code: string;
  active: boolean;
}

export interface TitleSetting {
  id: string;
  name: string;
  active: boolean;
}

export interface WorkplaceSetting {
  id: string;
  name: string;
  active: boolean;
}

